En esta clase se implementa la resolución de ecuaciones cuadráticas mediante la fórmula general, aplicando Programación Orientada a Objetos para estructurar y reutilizar el código de forma eficiente.

Información de la clase - Elaborada por el Ing. Alejandro Cruz Silva :
FORMAS DE CONCATENAR VALORES
#Agregando una coma y colocando la variable
#Esto hará que se coloque al final del mensaje con un espacio
print("Tu número fue el: ",n)

#Usa f-string para concatenar
#Una f-string (formatted string literal) te perminte insertar variables directamente
#dentro de una cadena sin necesidad de usar el operador + para concatenar a la función str.format().
print(f"Tu número fue el: {n}")

#Usando el operador +
print("Tu número fue el: "+n)